SWOT и блоки

1. Добавлен frontend/src/lib/blockDefaults.ts: дефолты по типу блока, normalizeSwotData, сброс rich_content при смене типа.
2. Исправлен isRichBlock — TipTap больше не показывается вместе с SWOT.
3. При редактировании SWOT данные нормализуются.

Markdown

1. Общий компонент MarkdownPreview.tsx с react-markdown + GFM (таблицы).
2. Preview в редакторе и просмотр блока используют один рендер.
3. Стили в index.css для .markdown-body.
4. Подключён @tailwindcss/typography.

TipTap

1. Синхронизация контента при редактировании (useEffect + setContent).
2. Таблицы 3×3, добавление/удаление строк.
3. Русские подсказки тулбара.

Русификация

1. Центральный файл frontend/src/i18n/ru.ts.
2. Обновлены BlockModal, SmartBlockEditors, BlockRenderer, BusinessPlanDetailsPage, BusinessPlansPage, index.html.